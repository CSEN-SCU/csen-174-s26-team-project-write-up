# Webapp currently uses Vite dev proxy defaults in `vite.config.js`.
# These vars are optional placeholders for future API base-url overrides.
# Shared URLs live in root `.env.example`:
# - APP_API_BASE_URL
# - COACHING_API_BASE_URL
